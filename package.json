{
  "name": "botim-limit-orders",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc -b && vite build",
    "typecheck": "tsc -b",
    "preview": "vite preview",
    "test": "esbuild tests/engine.test.ts --bundle --platform=node --format=esm --outfile=node_modules/.tmp/engine.test.mjs --log-level=warning && node node_modules/.tmp/engine.test.mjs"
  },
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@types/react": "^18.3.12",
    "@types/react-dom": "^18.3.1",
    "@vitejs/plugin-react": "^4.3.1",
    "typescript": "^5.6.3",
    "vite": "^5.4.10",
    "esbuild": "^0.21.5"
  }
}
